# Copy to .env and fill in. `vercel dev` loads .env automatically.
# For production: add the same key in the Vercel project settings
# (or run: vercel env add ANTHROPIC_API_KEY).
#
# Get a key at https://console.anthropic.com/ (API Keys).
ANTHROPIC_API_KEY=sk-ant-...
